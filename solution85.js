//two funcs getmax1 & getmax2 be equivalent and return the same value. Can you spot the prob. and fix it.

function getMax1() {
  var max = {
   name: 'Max Headroom'
  }
  return max;
}

function getMax2(){
 return {
    name: 'Max Headroom'
  }
}